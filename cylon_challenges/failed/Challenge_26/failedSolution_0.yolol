:o*=b:o++b=1c=1/(:i>6and:i!=22)d=:i%2:i=notd*(:i/2)+d*(:i*3+1)goto1
:o+=(:i>1)+(:i>3)+(:i==3)*6+(:i>4)*3+(:i>5)*3+(:i>6)*7b=0goto++:done

//Submission to Cylon challenge Conjuring Collatz (26)

//Find the length of the collatz sequence for a given number.
//
The collatz sequence is generated by repeatedly applying the following:

```if (value is even)
    value = value / 2
else
    value = 3 * value + 1```
    
The sequence ends when the value equals 1.

//Fail exception: "Failed test case"

